# Node modules (both in client and server)
zylo/node_modules/
server/node_modules/

# Build directories (if using build tools in either part)
zylo/dist/
server/dist/

# .env files
*.env
*.env.local
*.env.*.local

# Log files
*.log
